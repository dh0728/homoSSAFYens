<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 선택됨 -->
    <item android:state_checked="true" android:color="@color/ocean_blue"/>
    <!-- 기본 (선택 안 됨) -->
    <item android:color="@android:color/white"/>
</selector>
